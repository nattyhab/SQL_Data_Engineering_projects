CREATE or REPLACE TABLE mai.x (
    id INTEGER PRIMARY KEY,
    name VARCHAR,
    lvl VARCHAR
)
INSERT INTO (id,name,lvl)
VALUES
    (1,asd,1),
    (2,wef,4),
    (3,dgv,7)
-----------------------------------------------------

CREATE OR REPLACE TABLE stg.